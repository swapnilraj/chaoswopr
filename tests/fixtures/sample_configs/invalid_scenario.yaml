# This file is intentionally invalid for testing schema validation
name: invalid_scenario
# Missing required fields: version, hypothesis, fault_sequence, slo_thresholds, blast_radius, success_criteria

# Also has invalid values
fault_sequence: "not an array"
slo_thresholds:
  finality_delay_max_epochs: -1  # negative not allowed
